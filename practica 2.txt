hostnamectl
ping nodo2
ping nodo1
sudo systemctl restart pcsd
sudo pcs host auth nodo1 nodo2 -u hacluster
sudo pcs cluster setup ClusterLeury nodo1 nodo2
sudo pcs cluster start --all
sudo pcs status
cat /etc/hosts
sudo nano /etc/hosts
sudo pcs property set stonith-enabled=false
sudo pcs property set no-quorum-policy=ignore
sudo pcs property
sudo pcs cluster enable --all
sudo pcs status
sudo pcs resource create ClusterIP ocf:heartbeat:IPaddr2 ip=192.168.1.150 cidr_netmask=24 op monitor interval=30s
sudo pcs status resources
ping 192.168.1.150
sudo pcs resource cleanup ClusterIP
sudo pcs resource enable ClusterIP
sudo pcs resource debug-start ClusterIP
ip a
ip route
sudo systemctl stop pacemaker
sudo pcs status nodes
sudo pcs status resources
sudo systemctl start pacemaker
sudo pcs status nodes
sudo pcs status resources
sudo pcs resource cleanup ClusterIP
sudo pcs resource enable ClusterIP
sudo pcs resource debug-start ClusterIP
ping 192.168.1.150
sudo systemctl stop pacemaker
ping 192.168.1.150
sudo pcs status resources
sudo pcs resource cleanup ClusterIP
sudo pcs resource enable ClusterIP
sudo pcs resource debug-start ClusterIP
ping 192.168.1.150
sudo pcs status resources
sudo systemctl start pacemaker
sudo pcs status nodes
sudo pcs status resources